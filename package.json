{
  "name": "lore-pos-server",
  "version": "1.0.1",
  "description": "Backend + Frontend para Sistema POS - Restaurante LORE",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "pos",
    "restaurant",
    "lore"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express-rate-limit": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}